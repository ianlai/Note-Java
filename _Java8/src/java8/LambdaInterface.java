package java8;

public interface LambdaInterface {
	int operation(int a, int b);  //we can only have one unimplement method if we want to use lambda
	//int operation2(int a, int b, int c);  //even different name or different number of arguments are not allowed 
}
